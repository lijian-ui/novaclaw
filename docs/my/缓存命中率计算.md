DeepSeek API 原始 response
┌──────────────────────────────────┐
│ usage.prompt_tokens              │ ← 本次输入 token 数
│ usage.completion_tokens          │ ← 本次输出 token 数
│ usage.prompt_cache_hit_tokens    │ ← 缓存命中的 token 数
└──────────┬───────────────────────┘
           │
           ▼
client.rs:L279-L283 — 提取字段
┌──────────────────────────────────┐
│ cached = prompt_cache_hit_tokens │ ← 优先 DeepSeek 专用字段
│       .or(cached_tokens)         │ ← 降级通用字段
│ cached_tokens: 25344             │
└──────────┬───────────────────────┘
           │ StreamEvent::Usage
           ▼
runtime.rs:L793-L805 — 累计
┌──────────────────────────────────┐
│ total_input_tokens  += 5479      │ → 累计 25932
│ cache_hit_tokens    += 25344     │ → 累计 25344
│ cache_miss_tokens   += 588       │ → 累计 588
│   ↑ 逻辑：cached>0 时            │
│     miss = input - cached        │
│   cached=0 时                    │
│     miss = input                 │
└──────────┬───────────────────────┘
           │
           ▼
session.rs:L171-L180 — 计算
┌──────────────────────────────────┐
│ cache_hit_rate =                 │
│   cache_hit_tokens               │ → 25344
│   ────────────────────────       │
│   hit + miss                     │ → 25344+588=25932
│ = 97.7%                          │ ✅
└──────────────────────────────────┘


公式很简单 ： cache_hit_rate = cache_hit_tokens / total_input_tokens

用的字段 ：DeepSeek API 返回的 usage.prompt_cache_hit_tokens （这个是 DeepSeek 精确前缀缓存的专用字段）

验证日志 ：